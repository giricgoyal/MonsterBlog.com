/*void setup() {
  size(1320,384);
  background(0,180);
  
  // Copy this statement-----------------------------------------------------------
  initWordCram(1890,2012);  // replace the years with the year variables from the slider
  
}

void draw() {
  
  // copy this statement----------------------------------------------------------
  drawMe(1890,2012);  // replace the years with the year variables from the slider
} */
